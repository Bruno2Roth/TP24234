@{
    ViewData["Title"] = "Gr";
}

<div>

    @if(ViewBag.Error){
        <h1>Verifique su usuario y contraseña</h1>
    }
    <form action='@Url.Action("Login", "Home")' method="POST" enctype="multipart/form-data" required>

        Usuario: <input type="text" name="user" required><br/>
        Contraseña: <input type="password" name="password" required>

        <input type="submit" value="Iniciar Sesión" />

    </form>

</div>
